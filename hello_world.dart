/**
 * * Informação IMPORTANTE é detacada
 * ! Método DEPRECIADO, não usar
 * ? Este metodo deveria ser exposto em uma API puiblica?
 * TODO Refatorar este método conforme o uso de API
 * // Nao sei pra que usar isso
*/
main() {
  print('Hello World!');
}
